%h1 Listing posts

%table
  %tr
    %th Title
    %th Body
    %th Published
    %th
    %th
    %th

  - @posts.each do |post|
    %tr
      %td= post.title
      %td= post.body
      %td= post.published

      %td
        = link_to 'Show', post, {:id => "show_#{post.title}"}
      %td= link_to 'Edit', edit_post_path(post)
      %td= link_to 'Destroy', post, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Post', new_post_path
